worker_processes:
  - "127.0.0.1:8809"
  - "/tmp/uvicorn.sock"

debug_port: 7777
rpc_address: '127.0.0.1:8089'

#-------------------------------------

data_dir: "dd"
config_dir: "cd"

producer-name: eosio
enable-stale-production:

trace-no-abis:

disable-replay-opts:

plugin: eosio::state_history_plugin
chain-state-history:
trace-history:
